name: iOS Build (GitHub Actions)

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    name: Build iOS IPA
    runs-on: macos-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4

    - name: Make gradlew executable
      run: chmod +x gradlew

    - name: Build iOS App
      run: |
        # Pre-build Kotlin framework to ensure it's available for Xcode
        ./gradlew :shared:assembleRelease --no-daemon
        
        cd iosApp
        # Disable sandboxing and use manual path for the framework if needed
        xcodebuild -project iosApp.xcodeproj \
                   -scheme iosApp \
                   -configuration Release \
                   -sdk iphoneos \
                   -derivedDataPath build \
                   ENABLE_USER_SCRIPT_SANDBOXING=NO \
                   CODE_SIGNING_ALLOWED=NO \
                   CODE_SIGNING_REQUIRED=NO \
                   CODE_SIGN_IDENTITY="" \
                   AD_HOC_CODE_SIGNING_ALLOWED=YES

    - name: Package .ipa
      run: |
        mkdir -p Payload
        cp -r iosApp/build/Build/Products/Release-iphoneos/iosApp.app Payload/
        zip -r iosApp.ipa Payload
        
    - name: Upload iOS IPA Artifact
      uses: actions/upload-artifact@v4
      with:
        name: ios-app-ipa
        path: iosApp.ipa
        retention-days: 7
